# understanding poisson's distribution from khan academy
# using `dpois`, `ppois` and `rpois` in R
# the probability of observing 5 cars when lambda = 7
dpois(5, 7)

# the probability of observing 1 or less than 1 cars
ppois(26, 7)

# random pois
rpois(10, 7)
